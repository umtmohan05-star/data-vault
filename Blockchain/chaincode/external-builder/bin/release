#!/bin/bash
set -euo pipefail
BUILD_OUTPUT_DIR=$2
RELEASE_OUTPUT_DIR=$3
cp "$BUILD_OUTPUT_DIR/connection.json" "$RELEASE_OUTPUT_DIR/connection.json"
if [ -d "$BUILD_OUTPUT_DIR/metadata" ]; then
    cp -a "$BUILD_OUTPUT_DIR/metadata" "$RELEASE_OUTPUT_DIR/"
fi
exit 0
